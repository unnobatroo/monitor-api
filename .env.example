# app database connection (used by FastAPI + Alembic)
DATABASE_URL=postgresql://user:password@db:5432/grid_db

# optional local overrides for sensor services
API_BASE_URL=http://api:8000
SENSOR_NODE_ID=1
SEND_INTERVAL_SECONDS=8
